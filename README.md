# Голосовой помощник

Голосовой помощник с интеграцией GigaChat и Salute Speech API.

## Возможности

- 🎤 Распознавание речи (Speech-to-Text) через Salute Speech
- 🤖 Обработка запросов через GigaChat AI
- 🔊 Синтез речи (Text-to-Speech) через Salute Speech
- 💬 Полный цикл: голос → текст → ИИ → ответ → голос

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Создайте файл `.env` на основе `env.example`:
```bash
cp env.example .env
```

3. Заполните `.env` файл вашими API ключами:
```
GIGACHAT_AUTH_KEY=your_gigachat_auth_key_here
SALUTE_SPEECH_AUTH_KEY=your_salute_speech_auth_key_here
```

## Запуск

### Быстрый запуск (рекомендуется)

Просто запустите скрипт:
```bash
./run.sh
```

Скрипт автоматически:
- Проверит наличие `.env` файла
- Установит зависимости при необходимости
- Запустит бэкенд на порту 5000
- Запустит фронтенд на порту 8000

### Ручной запуск

1. Запустите бэкенд:
```bash
cd backend
python app.py
```

2. В другом терминале запустите фронтенд:
```bash
cd frontend
python -m http.server 8000
```

3. Откройте в браузере: `http://localhost:8000`

## Использование

1. При первом использовании браузер запросит разрешение на доступ к микрофону - нажмите "Разрешить"
2. Нажмите и удерживайте кнопку микрофона для записи
3. Отпустите кнопку - запись остановится
4. Дождитесь обработки (распознавание → ИИ → синтез)
5. Нажмите "Воспроизвести ответ" для прослушивания

**Примечание:** Для работы требуется стабильное интернет-соединение, так как обработка происходит на серверах Sberbank.

## API Endpoints

- `GET /api/health` - Проверка работоспособности
- `POST /api/transcribe` - Распознавание речи
- `POST /api/chat` - Запрос к GigaChat
- `POST /api/synthesize` - Синтез речи
- `POST /api/voice-assistant` - Полный цикл обработки

## Структура проекта

```
.
├── backend/
│   └── app.py          # Flask приложение
├── frontend/
│   └── index.html      # Веб-интерфейс
├── requirements.txt    # Python зависимости
├── env.example         # Пример конфигурации
└── README.md           # Документация
```
